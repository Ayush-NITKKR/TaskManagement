# Backend

Express and MongoDB backend for the EdTech task platform.

## Run

```bash
npm install
npm run dev
```

The backend listens on `http://localhost:4000`.

## Notes

- API base: `/api/v1`
- Swagger docs: `/api-docs`
- Serves the built React frontend from `../frontend/dist`
